<%= form_for ([@question, @question.answers.build]) do |f| %>
  <p>
    <%= f.label :body %>
    <%= f.text_area :body, :maxlength => "200" %>
  </p>
  <p>
    <%= f.submit %>
  </p>

<%= flash[:notice] %>


<% end %>
