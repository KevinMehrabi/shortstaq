<%= answer.user.name if answer.user %>

<%= answer.body %>

<%= link_to 'Delete Answer', [answer.question, answer],
            method: :delete,
            data: { confirm: "are you sure you want to delete this?"}  %>
